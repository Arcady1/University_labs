#include <iostream>
using namespace std;

class Point
{
    private:
        int x;
        int y;
    public:
        Point( int x_, int y_ ) : x(x_), y(y_) {};
        int getX() { return x; };
        int getY() { return y; };
};

int main(int argc, char const *argv[])
{
    

    return 0;
}
